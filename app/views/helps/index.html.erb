<h1>Listing helps</h1>

<table>
  <tr>
  </tr>

<% @helps.each do |help| %>
  <tr>
    <td><%= #link_to 'Mostrar', help %></td>
    <td><%= link_to 'Editae', edit_help_path(help) %></td>
    <td><%= link_to 'Eliminar', help, :confirm => 'Esta Seguro?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= #link_to 'New help', new_help_path %>