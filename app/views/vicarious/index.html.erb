<%if current_user%>
<h1>Vicarias</h1>

<% form_tag vicarious_path, :method => 'get' do %>
  <p>
    Parametro: <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Buscar", :name => nil %>
</p>
<% end %>
<table>
  <tr>
    <td>
      <DIV align="right">   

<%= button_to 'Crear Vicaria', new_vicariou_path %>

</div>
<tr>
    <td>
<table border="3">
  <tr id ='head-table'>
    <th>Nombre Vicaria</th>
    <th>Ubicacion</th>
    <th>Telefono</th>
    <th colspan="3"> Acciones</th>
  </tr>

<% @vicarious.each do |vicariou| %>
  <%  if vicariou.state %>  
      <tr>
        <td><%= link_to  vicariou.name_vicariou,vicariou %></td>
        <td><%=h vicariou.ubication %></td>
        <td><%=h vicariou.telephone %></td>
        <td><%= link_to 'Editar', edit_vicariou_path(vicariou) %></td>
        <td><%= link_to 'Eliminar', vicariou, :confirm => 'Esta seguro que desea eliminar esta vicaria? ', :method => :delete %></td>
      </tr>
    <%end%>
  <% end %>
</table>
</td>
  </tr>
</table>

<br />
<%else%>
  <h1>no puede realizar esta accion, porfavor contactese</h1>
<%end%>
