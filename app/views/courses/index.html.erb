<h1>Cursos</h1>

<p>
<% form_tag courses_path, :method => 'get' do %>
      Nombre <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Buscar", :name => nil %>
      <% end %>
</p>
 
<table>
  <tr align="right">
    <td>  
      <%=button_to  'Crear Curso', new_course_path %>
    </td>
  </tr>
  <tr>
    <td colspan="2">
 
    <table border="3">
      <tr id = 'head-table'>      
        <th>Nombre</th>
        <th WIDTH=250>Descripción</th>
        <th colspan="3">Accion</th>
      </tr>

      <% @courses.each do |course| %>
        <tr>
          <td><%= link_to course.name, course %></td>
          <td WIDTH=250><%=h course.description %></td>
<!--          <td><%= link_to 'Ver', course %></td>  -->
          <td><%= link_to 'Editar', edit_course_path(course) %></td>
<!--          <td><%= link_to 'Eliminar', course, :confirm => '¿Está seguro', :method => :delete %></td>  -->
        </tr>
      <% end %>
    </table>
    </td>
  </tr>
</table>
<br />