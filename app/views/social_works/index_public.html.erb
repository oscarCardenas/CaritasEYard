<div id="content-wrap">

  <div id="main" >
<h1>Obra Social</h1>

<p>
  <% form_tag  :controller => "social_works", :action => "index_public",:id =>'find', :method => 'get' do %>
    Parametro: <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Buscar", :name => nil %>
  <% end %>
</p>

<table>
  <tr>
    <td colspan="2">
      <table border ="3">
        <tr id="head-table">
          <th>Nombre</th>
          <th>Responsable</th>
          <th>Ubicacion</th>
          <th>Accion</th>
        </tr>
        <% @social_works.each do |social_work| %>
          <tr>
            <td><%=link_to (h social_work.name), :controller => "social_works", :action => "show_public", :id =>social_work %></td>
            <td><%=h social_work.responsible %></td>
            <td><%=h social_work.location %></td>
            <td><%= link_to 'Ver',  :controller => "social_works", :action => "show_public", :id =>social_work %></td>
          </tr>
        <% end %>
  </tr>
</table>
</td>
</tr>
</table>
<br />
      </div>
    <%= render :partial => "main_page/acordeon" %>
